# SPS 选股系统 — 用户使用说明

欢迎使用 SPS 参数化选股系统。本文件帮助你在自己的电脑上从零开始使用。

---

## 一、安装与启动

### 环境要求
- Windows 10/11（64位）
- 无需安装 Python 或任何开发环境

### 启动步骤
1. 解压收到的 `SPS` 文件夹到任意位置（建议 `D:\SPS`，**路径中不要有中文**）
2. 双击 `SPS.exe`
3. 首次启动会出现黑色控制台窗口（显示运行日志，请勿关闭），并自动打开浏览器访问 `http://127.0.0.1:5000`
   - 若浏览器未自动打开，手动在浏览器输入 `http://127.0.0.1:5000`

> 开发项目目录也可以直接双击 `启动SPS.bat`。它会自动选择打包版或开发版；
> 如果 SPS 已经运行，再次双击只会打开页面，不会重复占用端口。

### 首次使用必做：更新数据
系统启动后不含行情数据，请先点击页面右上角 **「⬇ 更新数据」**。系统优先使用 HiThink，失败的标的自动回退 AkShare；已有缓存只刷新最近的重叠窗口。之后每天收盘后更新一次即可，顶部会显示两种来源各自覆盖的标的数量。

---

## 二、AI 解读配置（可选，需你自己的 API Key）

系统内置 AI 解读功能：一键让大模型解读你的筛选结果。**AI 使用的 API Key 由你自己提供，费用走你自己的账户，系统本身不收取任何 AI 费用，Key 只保存在你本机。**

### 支持的大模型（任选其一）
| 服务商 | Base URL | 模型示例 |
|---|---|---|
| DeepSeek | `https://api.deepseek.com/v1` | `deepseek-chat` |
| OpenAI | `https://api.openai.com/v1` | `gpt-4o-mini` |
| 阿里通义 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `qwen-plus` |
| 月之暗面 Kimi | `https://api.moonshot.cn/v1` | `moonshot-v1-8k` |
| 智谱 GLM | `https://open.bigmodel.cn/api/paas/v4` | `glm-4-flash` |
| 本地 Ollama | `http://127.0.0.1:11434/v1` | `qwen2.5:7b`（免费，无需联网） |

### 配置步骤
1. 到上述任一服务商官网注册并创建 API Key（如 DeepSeek 官网 → API Keys → 创建）
2. 打开 SPS 页面 → 右上角 **「⚙ AI 设置」**
3. 也可直接在顶部 **「快速选择模型」** 下拉选服务商（DeepSeek / 智谱 GLM / 通义千问 / Kimi / OpenAI / OpenRouter / 本地 Ollama），接口地址和模型名会自动填好，你只需填 API Key
4. 点「🔗 测试连接」→ 显示 ✅ 即成功 → 「💾 保存」
5. 之后每次筛选出结果后，点结果区 **「🤖 AI解读」** 即可获得市场风格分析、重点关注标的与风险提示

> 注意：不配置 AI 也能正常使用全部筛选/回测/持仓功能，AI 解读是可选增强。

---

## 三、功能速览

### 🔍 筛选
- **① 选个风格，一键开筛**：8 种经过全市场回测验证的风格模板（稳健趋势/突破追涨/缩量回踩/放量启动/均线多头/横盘蓄势/强势龙头/三重保险组合），点卡片选中（可多选组合），点「开始筛选」执行
- **② 自定义指标（进阶）**：18 个指标自由勾选组合，每个指标带历史胜率徽章（↯绿≥55% 黄≥45% 红<45%）
- 结果分「已触发」（明天开盘是买点）与「接近触发」（差少数条件，提前关注）
- 行业分布图可点击过滤；结果可导出 CSV

### ⏪ 历史回放
选一个历史日期，重现"那天收盘系统会选出什么"，并统计其后20日真实收益（含成本）——检验任何条件组合的历史成色。

### 💼 持仓管理
买入后登记持仓，系统每日自动诊断：
- 🟢 健康 / 🟡 警惕 / 🔴 危险 三档信号灯
- 距止损价余量、MA20 上下方、距峰值回撤，一目了然
- 触发卖出规则自动提示（明日开盘执行）
- 「持仓自检回放」：回看你的买入日是否满足当前筛选条件，自检买入逻辑
- **📝 模拟 / 💰 实盘两种模式**：建议新手先用模拟模式跟踪，满 20 笔前界面保持"新手观察模式"（提示语以观察为主）

### 📋 模拟盘复盘报告
在持仓管理页用模拟模式登记并平仓后，可一键生成复盘：胜率、平均收益、最好/最差一笔、平均持有天数、退出原因分布。样本满 20 笔前不足以评价方法，报告会如实提醒。

### 🛡 合规与研究声明
- 首次启动会展示免责声明，需确认后使用（记录保存在本机）
- 界面所有"买点/止损"均为**历史数据研究参考**，不构成投资建议
- AI 解读已内置约束：只做数据归纳与风险提示，不输出方向性指令
- 模板介绍中的胜率数字为实时读取的最新回测统计（含样本数与统计截止日），不再使用固定文案

### 💬 问题反馈
顶栏「💬 反馈」按钮可提交问题与建议，保存在本机 `data/runs/feedback.log`（自动附数据状态快照），把该文件发给开发者即可。

### ⏰ 每日自动更新
顶栏可勾选「自动更新」：开启后每个交易日到达设定时间（默认 17:30，可在配置文件 `data/meta/app_config.json` 修改）自动增量刷新行情（只刷数据，约 3~5 分钟），无需手动点击。程序需处于运行状态。

### ⬇ 更新数据 vs 🧭 深度扫描
- **⬇ 更新数据**（日常用）：只刷新行情数据，快照快路径，通常 3~5 分钟。更新完即可正常筛选、回测、诊断持仓。
- **🧭 深度扫描**：更新数据 + 全市场形态检测 + 重建候选清单。形态检测是 CPU 计算，约 0.5~2 小时（已多进程并行，速度取决于电脑核数）；期间请勿关闭程序。只在需要最新形态候选时使用。
- 结果不受电脑性能影响：同样的数据在任何电脑上算出的候选清单一模一样，性能只决定等待时长。

---

## 四、常见问题

**Q: 双击 SPS.exe 后闪退？**
路径含中文或缺少写入权限。把文件夹移到如 `D:\SPS` 再试，或右键"以管理员身份运行"。

**Q: 更新数据很慢/失败？**
日常增量更新走"全市场快照"快路径（几次请求拉全部股票最新日K，通常几十秒内完成）；只有首次下载、缓存落后多日或个别缺漏的票才走逐票补历史（较慢）。交易时段 15:00-16:00 接口拥堵，建议 16:30 后更新或开启"自动更新"。

**Q: 筛选结果为 0？**
先确认数据已更新到最新交易日（顶部状态栏绿色 ✅）；再把条件放宽（如把量比从 2 降到 1.3）。状态接口会同时检查全市场覆盖率；若存在过期股票，会显示当前、过期和总股票数。

**Q: AI 解读报错 401/超时？**
检查 API Key 是否正确、账户是否有余额、Base URL 是否与服务商匹配。

**Q: 数据存在哪里？可以迁移吗？**
全部在 SPS 文件夹内的 `data/` 目录。拷贝整个文件夹到新电脑即可完整迁移。

---

## 五、风险提示

本系统是基于历史数据的技术面统计工具，所有胜率均为历史回测结果，**不代表未来表现**。股市有风险，本系统仅用于缩小观察范围、辅助决策，不构成任何投资建议，据此操作盈亏自负。
