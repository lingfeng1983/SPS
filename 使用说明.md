# SPS 参数化选股系统 · 使用说明

## 一句话
勾选指标 → 改参数（区间内）→ 全市场筛选 → 已触发=买点，接近=提前关注。

## 界面布局
```
┌─────────────┬──────────────────────────┐
│ 左：指标面板   │ 右：解读卡 + 结果列表        │
│ 18个指标×4类  │  ↯回测徽章 + 排序           │
└─────────────┴──────────────────────────┘
```

## 快速上手
1. 勾选指标（如"收盘价站上MA"）→ 该行参数解锁，其他保持锁定
2. 勾选后右侧出现**解读卡片**：指标含义、调参影响、回测实证
3. 每个指标旁的 **↯徽章** = 该参数历史20日胜率(样本数)
   绿≥55% / 黄≥45% / 红<45% —— 优先挑绿黄
4. 点「🎯 开始筛选」→ 全市场计算

## 结果两组
- **已触发**（绿标）：全部条件满足 = 今日买点
  卡片直接给：买点价 + 止损价
- **接近触发**（黄标）：差1-2个条件，标注缺哪个 → 提前关注

排序：满足度 / 代码 / 买点价，下拉切换。

## 参数有效区间
每个指标有失灵边界（如站上MA限5~120日），越界输入会变红，
提交时后端再次校验并拒绝。区间依据回测样本与统计意义划定。

## 策略管理
调好后「💾 保存为策略」→ 起名。
下次从「📁 我的策略」点击名字一键载入全部参数。

## 数据维护
- 「⬇ 更新全市场数据缓存」：拉取最新日线+财务（每天收盘后跑一次）
- 数据积累后重跑 `python scripts/backtest_params.py` 刷新↯徽章
  （当前96只样本，越多越准）

## 详情页
点结果卡片进入：K线（信号日/止损线）+ 基本面体检（5项红绿灯）
+ 当前条件逐条核对（✓/✗）+ 三大买卖数字

## 启动方式
双击 `D:\SPS\启动SPS.bat` → 浏览器打开 http://127.0.0.1:5000
